<!DOCTYPE html>
<html lang="en" class="scroll-smooth">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Safa Suiting</title>
  <link rel="shortcut icon" href="/assests/safaLogo.png" type="image/x-icon">
  <link rel="stylesheet" href="/styles/global.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper/swiper-bundle.min.css" />
  <script src="https://cdn.jsdelivr.net/npm/swiper/swiper-bundle.min.js"></script>

   <!-- AOS Library for Scroll Animations -->
   <!-- <link rel="stylesheet" href="https://unpkg.com/aos@next/dist/aos.css" /> -->


</head>

<body class="transition-all duration-300 bg-[#FFFFFF] text-gray-900 dark:bg-gray-900 dark:text-gray-100 !scroll-smooth">
  
 
  
  <header
    class=" border-b border-gray-500 bg-[#FFFFFF] text-black dark:bg-gray-900 dark:text-gray-100  dark:border-gray-700 fixed top-0 left-0 w-full z-50 ">
    <!-- Top Bar -->
    <div class="bg-black text-white text-sm py-2 px-4 flex justify-center items-center dark:bg-gray-800">
      <span class="mr-2">Unleash the Summer Vibes: Dive into Style with Our Hot Deals and Free Delivery!</span>
      <a href="#" class="underline">ShopNow</a>
    </div>

    <!-- Main Navigation -->
    <div class="flex justify-between items-center py-5 px-4 lg:px-24 md:px-8">
      <!-- Logo -->
      <!-- <div class="lg:text-2xl text-xl font-bold">Safa Suiting</div> -->
        <img src="/assests/safaLogo.png" alt="Safa Suiting" class="w-12 h-8 dark:brightness-0 dark:invert">
      


      <!-- Navigation Links -->
      <nav class="hidden md:flex md:space-x-6 text-base tracking-wide">
        <a href="/wishlist.html" class="nav-link active">Home</a>
        <a href="/contact.html" class="nav-link">Contact</a>
        <a href="/about.html" class="nav-link">About</a>
        <a href="/auth/register" class="nav-link">Sign Up</a>
      </nav>


      <!-- Search and Icons -->
      <div class="flex items-center space-x-4">
        <!-- Search Button for Small Screens -->
        <button class="text-black text-2xl -mr-8 md:hidden dark:text-white" id="searchIcon">
          <i class="bi bi-search"></i>
        </button>

        <!-- Search Bar for Medium and Large Screens -->
        <div class="relative group hidden md:block lg:block">
          <input type="text" id="desktopSearchInput" placeholder="Search..."
            class="block p-2 w-48 z-20 text-sm border border-slate-400 text-black bg-gray-100 focus:outline-none focus:ring-2 focus:ring-gray-300 dark:bg-gray-800 dark:text-white dark:focus:ring-gray-500 rounded-md">
          <button class="absolute top-0 right-0 p-2 text-sm font-medium h-full text-black dark:text-white">
            <i class="bi bi-search"></i>
          </button>
          <div id="desktopSearchResults"
            class="hidden mt-2 bg-white border border-gray-300 rounded-md shadow-lg absolute z-30 w-48  dark:bg-gray-800 dark:border-gray-600">
            <!-- Search results will appear here -->
          </div>
        </div>

        <button>
          <a href="/wishlist.html" class="relative hidden md:block cursor-pointer" id="wishlist">
            <i class="bi bi-heart text-2xl dark:text-white"></i>
            <span class="absolute -top-1 -right-1 bg-[#DB4444] dark:bg-[#de2d2d] text-white text-xs rounded-full px-1">3</span>
          </a>
        </button>

        <button>
          <a href="/my-cart" class="relative hidden md:block cursor-pointer" id="cart">
            <i class="bi bi-cart3 text-2xl dark:text-white"></i>
            <span class="absolute -top-1 -right-1 bg-[#DB4444] dark:bg-[#de2d2d] text-white text-xs rounded-full px-1">5</span>
          </a>
        </button>

        <!-- Account Icon -->
        <button>
          <a class="relative hidden md:block cursor-pointer" id="accountIcon">
            <i class="bi bi-person text-3xl text-black dark:text-white"></i>
          </a>
        </button>

        <!-- Account Dropdown Menu -->
        <div id="accountMenu"
          class="hidden absolute right-11 mt-72 min-w-56 bg-white shadow-lg rounded-md text-black w-48 dark:bg-gray-800 dark:text-white dark:shadow-gray-700">
          <ul>
            <li>
              <a href="/account.html" class="block p-2 hover:bg-gray-100 dark:hover:bg-gray-700">
                <i class="bi bi-person mr-2"></i> Manage My Account
              </a>
            </li>
            <li>
              <a href="/my-orders" class="block p-2 hover:bg-gray-100 dark:hover:bg-gray-700">
                <i class="bi bi-bag mr-2"></i> My Order
              </a>
            </li>
            <li>
              <a href="/my-cancellations" class="block p-2 hover:bg-gray-100 dark:hover:bg-gray-700">
                <i class="bi bi-x-circle mr-2"></i> My Cancellations
              </a>
            </li>

            <li>
              <a href="/logout" class="block p-2 hover:bg-gray-100 dark:hover:bg-gray-700">
                <i class="bi bi-box-arrow-right mr-2"></i> Logout
              </a>
            </li>
          </ul>
        </div>
      </div>
    </div>

    <!-- Hidden Search Bar for Small Screens -->
    <div id="mobileSearch"
      class="hidden p-4 bg-gray-100 border-t dark:bg-gray-800 dark:border-gray-600 fixed  w-full z-40">
      <input type="text" id="mobileSearchInput" placeholder="Search..."
        class="block p-2.5 w-full text-sm text-black border-2 border-black bg-white focus:outline-none focus:ring-2 focus:ring-gray-300 dark:bg-gray-700 dark:text-white dark:border-gray-600 dark:focus:ring-gray-500">
      <div id="mobileSearchResults"
        class="hidden mt-2 bg-white border  border-gray-300 rounded-md shadow-lg dark:bg-gray-800 dark:border-gray-600">
        <!-- Search results will appear here -->
      </div>
    </div>

    <!-- Bottom Navigation for Small Screens -->
    <div
      class="border-t fixed bottom-0 left-0 w-full z-20 bg-white shadow-md md:hidden flex justify-around py-2 dark:bg-gray-900 dark:border-gray-700">
      <a href="/" class="flex flex-col items-center text-black dark:text-white">
        <i class="bi bi-house text-xl"></i>
        <span class="text-xs">Home</span>
      </a>
      <a href="/wishlist.html" class="relative flex flex-col items-center text-black dark:text-white">
        <i class="bi bi-heart text-xl"></i>
        <span class="absolute -top-1 right-1 bg-[#DB4444] dark:bg-[#de2d2d] text-white text-xs rounded-full px-1">3</span>
        <span class="text-xs">Wishlist</span>
      </a>
      <a href="/my-cart" class="px-3 relative flex flex-col items-center text-black dark:text-white">
        <i class="bi bi-cart3 text-xl"></i>
        <span class="absolute -top-1 right-2 bg-[#DB4444] dark:bg-[#de2d2d] text-white text-xs rounded-full px-1">5</span>
        <span class="text-xs">Cart</span>

      </a>
      <a href="/account.html" class="flex flex-col items-center text-black dark:text-white">
        <i class="bi bi-person text-xl"></i>
        <span class="text-xs">Sign Up</span>
      </a>
    </div>
  </header>



  <script>


    const accountIcon = document.getElementById('accountIcon');
    const accountMenu = document.getElementById('accountMenu');

    accountIcon.addEventListener('click', () => {
      accountMenu.classList.toggle('hidden');
    });

    // Optional: Close menu if user clicks outside
    document.addEventListener('click', (e) => {
      if (!accountIcon.contains(e.target) && !accountMenu.contains(e.target)) {
        accountMenu.classList.add('hidden');
      }
    });

    const searchIcon = document.getElementById('searchIcon');
    const mobileSearch = document.getElementById('mobileSearch');
    const mobileSearchInput = document.getElementById('mobileSearchInput');
    const mobileSearchResults = document.getElementById('mobileSearchResults');
    const desktopSearchInput = document.getElementById('desktopSearchInput');
    const desktopSearchResults = document.getElementById('desktopSearchResults');

    // Dummy data for search
    const dummyResults = [
      'Summer Collection 2023',
      'Men’s Formal Suits',
      'Casual Wear for Women',
      'Trending Accessories',
      'New Arrivals: Kids Wear',
      'Top Picks for Winter'
    ];

    // Toggle mobile search visibility
    searchIcon.addEventListener('click', () => {
      mobileSearch.classList.toggle('hidden');
    });

    // Function to handle search input and display results
    function handleSearch(input, resultsContainer) {
      const query = input.value.toLowerCase();
      resultsContainer.innerHTML = ''; // Clear previous results

      if (query) {
        const filteredResults = dummyResults.filter(item =>
          item.toLowerCase().includes(query)
        );
        if (filteredResults.length > 0) {
          filteredResults.forEach(result => {
            const resultItem = document.createElement('div');
            resultItem.textContent = result;
            resultItem.className = 'p-2 hover:bg-gray-100 dark:hover:bg-gray-700 cursor-pointer';
            resultsContainer.appendChild(resultItem);
          });
        } else {
          const noResult = document.createElement('div');
          noResult.textContent = 'No results found';
          noResult.className = 'p-2 text-gray-500';
          resultsContainer.appendChild(noResult);
        }
        resultsContainer.classList.remove('hidden');
      } else {
        resultsContainer.classList.add('hidden');
      }
    }

    // Event listeners for mobile and desktop search inputs
    mobileSearchInput.addEventListener('input', () =>
      handleSearch(mobileSearchInput, mobileSearchResults)
    );

    desktopSearchInput.addEventListener('input', () =>
      handleSearch(desktopSearchInput, desktopSearchResults)
    );

    // Hide results when clicking outside
    document.addEventListener('click', (e) => {
      // Close mobile search results
      if (
        !mobileSearch.contains(e.target) &&
        !mobileSearchResults.contains(e.target) &&
        e.target !== searchIcon
      ) {
        mobileSearchResults.classList.add('hidden');
      }

      // Close desktop search results
      if (
        !desktopSearchInput.contains(e.target) &&
        !desktopSearchResults.contains(e.target)
      ) {
        desktopSearchResults.classList.add('hidden');
      }
    });

    // Ensure mobile search is hidden on larger screens
    window.addEventListener('resize', () => {
      if (window.innerWidth >= 768) { // Tailwind `md` breakpoint is 768px
        mobileSearch.classList.add('hidden'); // Hide the mobile search bar
        mobileSearchResults.classList.add('hidden'); // Hide mobile search results
      }
    });

    document.querySelectorAll('.nav-link').forEach(link => {
      link.addEventListener('click', () => {
        document.querySelectorAll('.nav-link').forEach(link => link.classList.remove('active'));
        link.classList.add('active');
      });
    });

  </script>
</body>

</html>