<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= error.status === 404 ? '404 - Page Not Found' : '500 - Server Error' %></title>
  <link rel="stylesheet" href="/styles/global.css">
</head>
<body class="bg-[var(--color-gray-50)]">

<%- include('./shared/header') %>

<main class="container mx-auto px-4 py-12 mt-20">
  <div class="max-w-3xl mx-auto text-center">
    <h2 class="text-5xl md:text-7xl lg:text-8xl font-bold text-[var(--color-black)] mb-6 animate-fade-in">
      <%= error.status === 404 ? '404' : '500' %>
    </h2>
    <h3 class="text-2xl md:text-3xl font-semibold text-[var(--color-black)] mb-6">
      <%= error.status === 404 ? 'Page Not Found' : 'Server Error' %>
    </h3>
    <p class="text-lg text-[var(--color-gray-600)] mb-10 md:mb-12 lg:mb-16">
      <%= error.status === 404 ? 'The page you’re looking for doesn’t exist. Return to the home page?' : 'Something went wrong on our end. Please try again later.' %>
    </p>
    <a 
      href="/" 
      class="inline-block px-6 py-3 md:px-10 bg-[var(--color-red-500)] text-[var(--color-white)] font-semibold rounded-lg hover:bg-[var(--color-red-700)] focus:outline-none focus:ring-2 focus:ring-[var(--color-red-500)] transition-all duration-300 shadow-md hover:shadow-lg">
      Back to Home Page
    </a>
  </div>
</main>

<%- include('./shared/footer') %>

<style>
  .animate-fade-in {
    animation: fadeIn 0.5s ease-in-out;
  }
  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }
</style>

</body>
</html>